import {
    colorPalette,
    sidebarTextarea,
    sidebarTextInput,
    sidebarUploadImage,
} from "@components/sidebar/components/settings";

export interface SectionProductType {
    btnText: sidebarTextInput;
    btnColor: colorPalette;
    bgColor: colorPalette;
    titleColor: colorPalette;
    captionColor: colorPalette;
}