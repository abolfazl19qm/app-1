import {
    colorPalette,
} from "@components/sidebar/components/settings";

export interface SectionBlogType {
    bgColor: colorPalette;
    timeColor: colorPalette;
    descriptionColor: colorPalette;
}